# Upstream Notice

The control intent and target-distance formula were adapted from the private
ROBOCON2025 file `Communication_Math/tf0/src/dpt/dpt/stb.py`. That file remains
in the user's main repository and was not copied into this workspace. The
adapter changes frame/topic ownership, quaternion yaw calculation, freshness and
map-lock gates, command serialization, and optional serial initialization.
